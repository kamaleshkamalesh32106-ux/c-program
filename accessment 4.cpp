#include<stdio.h>
int main(){
	int n,key;
	scanf("%d",&n);
	int arr[n];
	for(int i=0;i<n;i++);
      	scanf("%d",&arr[i]);
	scanf("%d",&key);
	for(int i=n-1;i<n;i--);
	    printf("%d",i);
	    return 0;
	{
		printf("-i");
		return 0;
		}    
	
}